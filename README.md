# proxy-checker
 A Python program that checks SOCKS5 and HTTPS proxies and determines whether they are functional. It is useful if you use publicly available free proxies and do not want to manually verify the functionality of each one.
 The program connects to the Telegram API, and if the proxy is not working, the connection will fail.
